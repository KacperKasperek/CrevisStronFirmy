"use client";
import { motion, useReducedMotion } from "framer-motion";
import { Check } from "@phosphor-icons/react";
import type { SiteContent } from "@/lib/content";
export default function Oferta({ content }: { content: SiteContent["offer"] }) {
  const reduce = useReducedMotion();
  return <section id="oferta" className="bg-mist px-6 py-20 sm:px-10 sm:py-28"><div className="mx-auto max-w-7xl"><h2 className="display text-center text-3xl text-rust sm:text-5xl">{content.title}</h2><div className="mt-14 grid items-stretch gap-7 md:grid-cols-3">{content.plans.map((plan, index) => <motion.article key={`${plan.name}-${index}`} initial={reduce ? false : { opacity: 0, y: 30 }} whileInView={{ opacity: 1, y: 0 }} viewport={{ once: true }} className={`relative flex flex-col rounded-3xl p-8 shadow-xl ${plan.featured ? "bg-ink text-paper ring-2 ring-rust md:-mt-4" : "bg-paper text-ink ring-1 ring-ink/5"}`}>{plan.featured && <span className="absolute -top-3 left-1/2 -translate-x-1/2 rounded-full bg-rust px-4 py-1 text-xs font-semibold uppercase text-paper">Najczęściej wybierany</span>}<h3 className="text-2xl font-bold">{plan.name}</h3><p className="mt-3"><span className="display text-5xl text-rust">{plan.price}</span> <span className="text-sm opacity-60">zł/mies.</span></p><ul className="mt-8 flex-1 space-y-4">{plan.features.map((feature, i) => <li key={`${feature}-${i}`} className="flex gap-3 text-[15px]"><Check size={20} weight="bold" className="shrink-0 text-rust" />{feature}</li>)}</ul><a href="#kontakt" data-track={`oferta-${plan.name}`} className="mt-9 inline-flex justify-center rounded-full bg-rust py-3.5 text-lg font-semibold text-paper hover:bg-rust-dark">{plan.cta}</a></motion.article>)}</div><div className="mt-20 border-t border-ink/15 pt-12"><h3 className="text-center text-xl font-bold sm:text-2xl">W każdym pakiecie otrzymujesz</h3><ul className="mx-auto mt-8 grid max-w-3xl gap-4 sm:grid-cols-2">{content.included.map((item, i) => <li key={`${item}-${i}`} className="flex gap-3 text-[15px]"><Check size={20} weight="bold" className="text-rust" />{item}</li>)}</ul></div></div></section>;
}
