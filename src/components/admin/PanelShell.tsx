import Link from "next/link";
import type { ReactNode } from "react";
import LogoutButton from "./LogoutButton";
const nav = [["/panel", "Pulpit"], ["/panel/messages", "Wiadomości"], ["/panel/analytics", "Statystyki"], ["/panel/projects", "Projekty"], ["/panel/content", "Treści"], ["/panel/settings", "Ustawienia"]];
export default function PanelShell({ title, eyebrow, children }: { title: string; eyebrow?: string; children: ReactNode }) { return <div className="min-h-screen bg-[#e9e7e2] text-ink"><header className="border-b-4 border-rust bg-ink text-paper"><div className="mx-auto flex max-w-[1500px] flex-wrap items-center justify-between gap-5 px-5 py-5"><Link href="/panel" className="display text-2xl">CREVIS / CONTROL</Link><nav className="flex flex-wrap gap-x-5 gap-y-2 text-sm font-semibold">{nav.map(([href, label]) => <Link key={href} href={href} className="hover:text-rust">{label}</Link>)}<LogoutButton /></nav></div></header><main className="mx-auto max-w-[1500px] px-5 py-10"><p className="font-mono text-xs uppercase tracking-[.22em] text-rust">{eyebrow ?? "Panel administracyjny"}</p><h1 className="display mt-2 text-4xl sm:text-6xl">{title}</h1><div className="mt-10">{children}</div></main></div>; }
