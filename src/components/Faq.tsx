import type { SiteContent } from "@/lib/content";
export default function Faq({ content }: { content: SiteContent["faq"] }) {
  return <section id="faq" className="bg-paper px-6 py-20 sm:px-10 sm:py-28"><div className="mx-auto max-w-4xl"><h2 className="display text-3xl sm:text-5xl">{content.title.split("\n").map((line) => <span className="block" key={line}>{line}</span>)}</h2><div className="mt-12">{content.items.map((item, i) => <details key={`${item.q}-${i}`} className="group border-b border-ink/10"><summary className="flex cursor-pointer list-none justify-between gap-6 py-6 text-left"><span className="text-lg font-bold text-rust sm:text-xl">{item.q}</span><span aria-hidden="true" className="shrink-0 text-3xl leading-none transition-transform group-open:rotate-45">+</span></summary><p className="pb-6 pr-10 text-[15px] leading-relaxed text-ink/70">{item.a}</p></details>)}</div></div></section>;
}
