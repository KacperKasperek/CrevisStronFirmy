import type { SiteContent } from "@/lib/content";
export default function DlaczegoMy({ content }: { content: SiteContent["why"] }) {
  return <section className="bg-mist px-6 py-20 sm:px-10 sm:py-28"><div className="mx-auto max-w-7xl"><h2 className="display text-center text-3xl text-rust sm:text-5xl">{content.title}</h2><p className="mx-auto mt-5 max-w-2xl text-center text-lg leading-relaxed text-ink/70">{content.description}</p><div className="mt-14 grid gap-7 sm:grid-cols-2 lg:grid-cols-4">{content.features.map((feature, index) => <article key={`${feature.title}-${index}`} className="rounded-3xl bg-paper p-8 shadow-[0_20px_50px_-30px_rgba(0,0,0,.4)] ring-1 ring-ink/5"><span aria-hidden="true" className="font-mono text-sm font-bold tracking-[.2em] text-rust">0{index + 1}</span><h3 className="mt-6 text-xl font-bold">{feature.title}</h3><p className="mt-3 text-[15px] leading-relaxed text-ink/70">{feature.text}</p></article>)}</div></div></section>;
}
